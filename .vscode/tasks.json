{
    "tasks": [
        {
            "label": "start",
            "type": "shell",
            "command": "pnpm run watch:start",
            "options": {
                "statusbar": {
                    "label": "$(play) Start Discord Bot",
                }
            }
        },
        {
            "label": "build",
            "type": "shell",
            "command": "pnpm run build",
            "options": {
                "statusbar": {
                    "label": "$(gear) Build Discord Bot",
                }
            }
        }
    ]
}
